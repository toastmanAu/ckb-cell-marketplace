[package]
name = "market-item-tests"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone test crate — not part of the root workspace.
[workspace]

[dependencies]
ckb-testtool = "1.1.1"
ckb-types = "1.1.0"
ckb-hash = "1.1.0"
molecule = "0.7.5"

[lib]
path = "src/lib.rs"
doctest = false
